# h7yoStudio

## Kyoto Guide

1. 不需安裝或建置：頁面直接透過 CDN 載入 React、htm 與 lucide-react。
2. 本機預覽：在專案根目錄執行 `python3 -m http.server 8000`，再開啟瀏覽器網址 `http://localhost:8000/kyoto-guide.html`。
3. 也可直接用瀏覽器開啟 `kyoto-guide.html`，即可使用分頁式的京都美食與景點指南。
